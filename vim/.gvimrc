" gvimrc loaded after vimrc

" => Plugins (for posterity)
    "Airline
    "AirlineTheme
    "gruvbox
    "nerdtree

" => Colors and text

    set guifont=Consolas:h14:cANSI:qDRAFT

" => Set default behaviour for key presses

" => UI Config

    set guicursor+=a:blinkon0 "disables cursor blinking - line already highlighted - only works in gVim
    set guioptions-=r " Disables default right-scroll-bar
    set guioptions-=R " Disables right-scroll-bar during window splits
    set guioptions-=l " Disables left-scroll-bar visually
    set guioptions-=L " Disables left-scroll-bar during window splits
    set guioptions-=T  "remove toolbar
    set guioptions-=m  "remove toolbar

" => Remappings

